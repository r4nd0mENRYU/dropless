{
  "name": "@dropless/sdk",
  "version": "0.1.0",
  "description": "TypeScript SDK for Dropless — zero-loss webhook delivery, with a Svix-compatible webhook verifier.",
  "type": "module",
  "license": "Apache-2.0",
  "repository": "https://github.com/r4nd0mENRYU/dropless",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "README.md"],
  "scripts": {
    "build": "tsc",
    "test": "tsc && node --test"
  },
  "keywords": ["webhooks", "svix", "dropless", "delivery"],
  "devDependencies": {
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=18"
  }
}
